(library
  (name middle_end_identifiers)
  (package ocamlc_kit)
  (libraries utils identifiers compiler_lambda compiler_clambda)
  (flags :standard -open Sexplib.Std -w -9)
  (preprocess (pps ppx_sexp_conv))
  )
